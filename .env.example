# Docker/Podman socket 路径
# 
# Docker (默认):        /var/run/docker.sock
# Podman (root):        /run/podman/podman.sock
# Podman (rootless):    /run/user/1000/podman/podman.sock
# Podman (OpenWrt):     /var/run/podman/podman.sock
#
# DOCKER_SOCKET=/var/run/docker.sock
